# MEMORY.md

Curated, durable memory — the things worth keeping across days. Daily files in
`daily/` are the raw stream; the distilled keepers land here (owner-reviewed,
never auto-merged). Empty to begin with.
